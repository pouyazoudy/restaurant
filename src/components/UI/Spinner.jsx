// Spinner.js
import React from 'react';
import classes from './Spinner.module.css'; // Make sure to import the CSS file

const Spinner = () => {
  return <div className={classes.spinner}></div>;
};

export default Spinner;
